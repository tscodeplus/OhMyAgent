# Allow esbuild post-install build scripts.
# pnpm 11+ blocks build scripts by default — this prevents ERR_PNPM_IGNORED_BUILDS
# at install time. ui/ is outside the root workspace, so root's pnpm-workspace.yaml
# allowBuilds does not apply here.
allowBuilds[esbuild]=true
