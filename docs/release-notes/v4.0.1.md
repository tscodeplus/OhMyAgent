v4.0.1 是补丁版本：内嵌 pi-mono 升级至 v0.85.1，修复 macOS 主窗口红绿灯按钮与侧栏的视觉冲突。

## ⬆️ 升级 pi-mono 至 v0.85.1

- 同步上游 agent/ai 源码（跨 v0.85.0 + v0.85.1）；本地补丁全部重放：工具循环守卫 + 粘性降级与上游新增的预执行中断检查合并，onStreamRetry / registerModel 注册表 / bedrock、codex、xai 类型断言保持不变
- 采纳上游 proxy.ts SSE 健壮性修复；@anthropic-ai/sdk 升至 ^0.123.0（anthropic-messages 迁移到 beta/messages 类型）
- 供应商模型目录同步 npm v0.85.1（新增 gpt-6-astra、deepseek-v4-flash-vision-exp）

## 🖥️ macOS 桌面修复

- **修复：主窗口红绿灯按钮位置与侧栏标题行冲突。** 放弃自定义 `traffic_light_position` 偏移，恢复 AppKit 默认位置；侧栏品牌行（Logo + 标题 + 折叠按钮）额外增加纵向间距
- 修复经 GitHub macOS runner 截图工作流视觉验证（临时工作流已移除，验证通过即清理）

***

v4.0.1 is a patch release: the embedded pi-mono is upgraded to v0.85.1, and the macOS main window's traffic lights no longer clash with the sidebar.

## ⬆️ pi-mono Upgraded to v0.85.1

- Synced upstream agent/ai sources (across v0.85.0 + v0.85.1); all local patches replayed — the tool-cycle guard + sticky fallback now merge with the new upstream pre-execution abort check, and onStreamRetry / registerModel registry / bedrock, codex, xai assertions stay intact
- Adopted the upstream proxy.ts SSE robustness fix; @anthropic-ai/sdk bumped to ^0.123.0 (anthropic-messages moves to beta/messages types)
- Provider model catalogs synced from the npm v0.85.1 tarball (adds gpt-6-astra, deepseek-v4-flash-vision-exp)

## 🖥️ macOS Desktop Fixes

- **Fixed: the traffic-light buttons clashed with the sidebar's brand row.** The custom `traffic_light_position` offset is dropped in favor of AppKit's default placement, and the sidebar brand row (logo + title + collapse toggle) gains extra vertical clearance
- Verified visually via a temporary macOS-runner screenshot workflow (removed after passing)
