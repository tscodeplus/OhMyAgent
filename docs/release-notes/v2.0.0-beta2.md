v2.0.0-beta2 fixes the release notes: Self-Harness is **enabled by default** (`harness.enabled: true`), not disabled.

***

v2.0.0-beta2 修正 release notes：Self-Harness **默认启用**（`harness.enabled: true`），而非关闭。
