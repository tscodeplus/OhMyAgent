v2.4.0-beta2 延续 beta1 的 Computer Use 重构，重点修复 Windows UIA 输入在实机上的真实可用性，并补充防抢焦点保护与 WebUI 截图投递。

## 🖥️ Computer Use（Windows UIA 实机修复）

- UIA 输入真正可用：修复此前"假成功"问题（SetFocus 聚焦、记事本窗口选择、元素真实命中）
- press-key 增加 IME 环境兼容：ValuePattern 追加路径，Win11 记事本经 document 子节点查找
- launch-app 超时竞态修复；按键查找改为窗口级 document，避免子窗口遗漏
- 新增防抢焦点保护与无触摸输入路径

## 🤖 Computer Use（工具定义）

- 工具描述改为可执行指引：工作流步骤、element_id 用法、截图语义说明

## 🖼️ WebUI

- send_screenshot 图片直接送达聊天
- /api/files/serve 允许根目录与 webui_send_media 对齐

## 🧪 测试

- 新增 scripts/verify-win-uia.ts 端到端桌面验证脚本

***

v2.4.0-beta2 continues the beta1 Computer Use rework, focusing on making Windows UIA input genuinely work on real desktops, plus focus-steal shields and WebUI screenshot delivery.

## 🖥️ Computer Use (Windows UIA verified fixes)

- UIA input actually works now: false-success paths fixed (SetFocus targeting, Notepad window picking, real element hits)
- press-key gained an IME-proof path: ValuePattern append, Win11 Notepad via document child lookup
- launch-app timeout race fixed; key lookup now scans the window-wide document to avoid missing sub-windows
- Foreground-steal shields and no-touch input paths added

## 🤖 Computer Use (tool definitions)

- Tool descriptions rewritten as actionable guides: workflow steps, element_id usage, screenshot semantics

## 🖼️ WebUI

- send_screenshot images are delivered straight into the chat
- /api/files/serve allowed roots aligned with webui_send_media

## 🧪 Tests

- Added scripts/verify-win-uia.ts end-to-end desktop verification script
