{
  "name": "ohmyagent-desktop",
  "version": "2.4.0-beta3",
  "description": "OhMyAgent Desktop — Tauri shell for local and remote Agent Gateway",
  "homepage": "https://github.com/tscodeplus/OhMyAgent",
  "author": "tscodeplus",
  "repository": {
    "type": "git",
    "url": "https://github.com/tscodeplus/OhMyAgent.git"
  },
  "type": "module",
  "scripts": {
    "dev:sidecar": "node scripts/dev-sidecar.cjs",
    "build:sidecar": "tsc -p sidecar/tsconfig.json && node scripts/copy-sidecar-static.cjs",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "tauri:build:win": "tauri build --bundles nsis",
    "tauri:build:mac": "tauri build --bundles app",
    "bundle-deps": "node scripts/bundle-deps.cjs"
  },
  "devDependencies": {}
}
